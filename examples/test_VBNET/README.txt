Eng.

How to work with example:
1. To use the library libximc.dll in the environment .NET wrapper is offered ximc/winX/wrappers/csharp/ximcnet.dll for operating systems of different bit sizes
2. Tested on platforms.NET from 2.0 to 4.5.1
3. To build and run the example, you need to use the Visual Studio 2013
4. The library with dependencies is located in the ximc/winX folders. For the example to work, you need the following files: xibridge.dll, libximc.dll
5. To run the compiled example, you need to copy all the dependencies specified in step 4 to the folder with the executable file



Rus.

Как работать с примером:
1. Для использования библиотеки libximc.dll в среде .NET предлагается обертка ximc/winX/wrappers/csharp/ximcnet.dll для операционной системы разной разрядности
2. Тестировалось на платформах .NET от 2.0 до 4.5.1
3. Для сборки и запуска примера нужно использовать среду Visual Studio 2013
4. Библиотека с зависимостями расположена в папках ximc/winX. Для работы примера необходимы файлы: xibridge.dll, libximc.dll
5. Для запуска скомпилированного примера необходимо все зависимости указанные в пунктe 4 скопировать в папку с выполняемым файлом
